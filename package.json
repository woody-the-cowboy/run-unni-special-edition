{
  "name": "superbmario",
  "version": "1.0.0",
  "description": "A supermario like clone",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel serve index.html",
    "build": "parcel build index.html --no-autoinstall --dist-dir build --no-source-maps --public-url ."
  },
  "author": "bRuttaZz",
  "license": "GPL-3.0",
  "dependencies": {
    "parcel": "^2.14.4"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.14.4",
    "@parcel/transformer-jsonld": "^2.14.4"
  }
}
